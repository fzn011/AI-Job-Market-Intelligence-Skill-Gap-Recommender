"""
job_clustering.py — Unsupervised job clustering module.

Responsibilities (planned):
    - Vectorise job descriptions using TF-IDF
    - Apply KMeans (or other) clustering to group similar jobs
    - Label each job with its cluster ID
    - Generate cluster summaries (dominant skills per cluster)
    - Provide helper to assign a new job to an existing cluster
"""

import pandas as pd


def cluster_jobs() -> str:
    """Placeholder for the full job clustering pipeline."""
    return "Job clustering module is ready."


def get_cluster_summary() -> str:
    """
    Placeholder — will return top skills per cluster once implemented.
    """
    return "Cluster summary: not yet implemented."
