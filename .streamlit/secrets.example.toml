# Copy to secrets.toml and fill in your values.
# Quote API keys that contain + or / characters.
USAJOBS_API_KEY = "+your-usajobs-api-key="
USAJOBS_USER_EMAIL = "your@email.com"

# Optional SMTP for weekly progress digest
SMTP_HOST = "smtp.gmail.com"
SMTP_PORT = 587
SMTP_USER = "your@gmail.com"
SMTP_PASSWORD = "your-app-password"
DIGEST_RECIPIENT = "your@gmail.com"
